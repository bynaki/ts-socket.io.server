{
  "name": "ts-socket.io.server",
  "version": "0.1.1",
  "description": "Typescript + Socket.IO Server",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": {
    "name": "bynaki",
    "email": "bynaki@icloud.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/bynaki/ts-socket.io.server.git"
  },
  "license": "MIT",
  "config": {
    "srcMain": "src/index.ts"
  },
  "scripts": {
    "name": "echo $npm_package_name",
    "start": "node .",
    "dev": "npm run build.tsc && npm start",
    "build.tsc": "tsc && bin/copy \"src/**/!(*.ts)\" \"dist\" \"**/node_modules/**\"",
    "build.parcel": "parcel build --target=node \"$npm_package_config_srcMain\" && bin/copy \"src/**/!(*.ts)\" \"dist\" \"**/node_modules/**\"",
    "clean": "rimraf dist",
    "archive": "tar cvfz archive.tar.gz dist/ package.json",
    "release": "npm run clean && npm run build.parcel && npm run archive && bin/release",
    "build.util": "tsc --outDir utils utils/index.ts",
    "build.test": "tsc -p tsconfig.test.json && bin/copy \"src/**/!(*.ts)\" \"dist.test/src\" \"**/node_modules/**\"",
    "test": "npm run build.test && ava",
    "test.watch": "npm run build.test && ava --watch",
    "clean.test": "rimraf dist.test",
    "fetch": "curl -o archive.tar.gz \"$RELEASE_URL\" && tar xvfz archive.tar.gz && npm install"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^7.2.7",
    "@types/node": "^9.6.18",
    "@types/socket.io-client": "^1.4.32",
    "ava": "^0.25.0",
    "glob": "^7.1.2",
    "socket.io-client": "^2.1.0",
    "typescript": "^2.8.3"
  },
  "dependencies": {
    "fourdollar.filewriter": "^1.0.0",
    "fourdollar.logger": "^1.0.3",
    "fourdollar.promisify": "^1.0.5",
    "jsonwebtoken": "^8.2.1",
    "socket.io": "^2.1.0",
    "socket.io-decorator": "^0.3.2"
  },
  "ava": {
    "files": [
      "dist.test/test/**/**.spec.js"
    ]
  }
}
